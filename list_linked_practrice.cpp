#include <bits/stdc++.h>
#include <stdio.h>
using namespace std;

int main(){
    // Node* head = NULL;
    // Node* temp = NULL;
    // temp = node->next;
    // temp = temp->next;
    // temp->next = head;
    // temp->val;
    // temp->next->val;
    
    return 0;
}