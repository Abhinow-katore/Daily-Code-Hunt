#include <bits/stdc++.h>
#include <iostream>
#include <vector>
using namespace std;
int main(){
    vector <string> vect{"geeks practice", "geeks idl", "geeks write"};
    for(auto &x: vect)
    cout<<x<<" ";
    return 0;
}


















// {
//     // //int a[10]; //array
//     // //vector is similar to array
//     // int n;
    // cin >> n;
    // vector<int> var{};
    // for (int i = 0; i < n; ++i)
    // {
    //     int x;
    //     cin >> x;
    //     var.push_back(x);
    //     cout<<var;
    // }
    

    // for (std::vector<char>::const_iterator i = var.begin(); i != path.end(); ++i)
    //     std::cout << *i << ' ';

