#include <bits/stdc++.h>
#include <stdio.h>
using namespace std;

int main(){
    int arr[8] = {2,5,6,4,1,3,9,8};
    int k;
    cin>>k;
    int arr1[k];
    int arr2[k];
    // int arr[10];
    for(int i = 0; i < sizeof(arr) / sizeof(arr[0]); i++){
            

       }
    
    return 0; 
}