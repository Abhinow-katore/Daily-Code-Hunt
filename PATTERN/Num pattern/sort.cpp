#include <bits\stdc++.h>
#include <stdio.h>
using namespace std;

int main()
{
    int k = 3;
    int arr[6] = {1, 2, 3, 4, 5, 6};
    int n = sizeof(arr) / sizeof(arr[0]);
    for (int j = 0; j < k; j++)
    {
        if (arr[j])
    }

    return 0;
}